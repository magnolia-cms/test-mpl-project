/*
// bypass one step
NestedPipeline {
  modules.BinaryCompatibility = null
}

// define a cron
NestedPipeline {
  cron = '@weekly'
}

// run a JS project (see 'js' branch)
NestedPipeline {}
*/

// custom step
NestedPipeline {
  stage('test') {
    echo "test"
  }
}
